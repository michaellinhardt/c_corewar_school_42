#include "ft_asm.h"

void	ft_parser(t_token *token, t_parser *parser)
{
	int state;

	state = DEBUT;
	while (state)
	{
		if (state == DEBUT)
		{

		}
		if (state == SHIFT)
		{


		}
		if (state == REDUCE)
		{


		}

	}
}
